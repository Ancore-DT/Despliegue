spring.application.name=empresa

# ============================================
# CONFIGURACION DE PERFILES
# ============================================
# Perfil por defecto: MySQL (desarrollo local)
# Para cambiar a PostgreSQL: spring.profiles.active=postgres
# Para Heroku: configurar SPRING_PROFILES_ACTIVE=postgres
spring.profiles.active=${SPRING_PROFILES_ACTIVE:mysql}

# ============================================
# NOTA: Las configuraciones específicas de base de datos
# se encuentran en:
# - application-mysql.properties (MySQL - desarrollo local)
# - application-postgres.properties (PostgreSQL - Heroku)
# ============================================
